Q. pairplot에서 스타일 지정이나 gitter가 가능한가?
A. pairplot에서의 지정은 어려운 것으로 보임. 데이터가 너무 헤비한 경우에 pairplot 분포가 매우 겹치는데,
   그런 경우에는 샘플링을 진행하여 그림.

Q. 연속형 변수를 범주화할 때, 현업에서는 함수를 따로 만드는지 loc 등의 기능을 사용하는지?
A. 데이터의 구간별 표본 갯수를 같게 만들어서 범주화해야하는 경우에 그에 맞게 구간을 자동으로 자르며 계산해주는
   함수를 따로 만드는 것이 일반적임. 하지만 보통 표본의 상태에 따라서 다르다!
